# indexer
```
$ git clone https://github.com/gsebold027/indexer.git
$ cd indexer
$ make -f Makefile
$ ./indexer
$ # Talvez precise dar permissão de execução para o arquivo indexer que é gerado do Makefile
$ # caso isso ocorra use o comando $ chmod 777 indexer 
```
